export class Sorting {
	keys: Key[];

	addKey(key: string): void {
		// Logic
	}

	sort(arr: any[]): void {
		// Logic
	}

	removeKey(key: string): void {
		// Logic
	}

	findKeyIndex(key: string): number {
		return 1;
	}
}

class Key {
	key: string;
	order: string;
}

enum Enums {
	ASC, DESC
}

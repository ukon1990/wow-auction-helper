import {TSMCSV} from '../../../utils/tsm/tsm-lua.util';

export class ProfitSummaryUtil {
  calculate(csvData: TSMCSV, realm: string, startDate: Date, endDate: Date) {
    console.log('Realm data', csvData);
  }
}
